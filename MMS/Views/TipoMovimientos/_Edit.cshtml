

@model MMS.Models.TipoMovimiento
@{
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
}

@Scripts.Render("~/bundles/jqueryval")
<script src="~/Scripts/Catalogos/TipoMovimientos.js"></script>

<script type="text/javascript">
    $.MMS.TipoMovimientos.init = function () {
        $.MMS.TipoMovimientos("Edit");
    };
</script>

@using (Html.BeginForm("Edit", "api/" + controllerName, FormMethod.Post, new { id = "frmTipoMovimientos" }))
{
    @Html.AntiForgeryToken()

    <div class="modal-header">
        <button type="button" class="close btn-link" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title">Modify Type of movements</h4>
    </div>

    <div class="modal-body">
        <div class="row clearfix">
           @Html.HiddenFor(model => model.TipoMovimientoID)
            <div class="col-md-12">
                <div class="form-group form-float">
                    <div class="form-line">
                        @Html.EditorFor(model => model.TipoMovimientoDesc, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.LabelFor(model => model.TipoMovimientoDesc, htmlAttributes: new { @class = "form-label" })
                    </div>
                    @Html.ValidationMessageFor(model => model.TipoMovimientoDesc, "", new { @class = "" })
                </div>
            </div>
        </div>
    </div>

    <hr class="m-b-0 m-t--10" />

    <div class="modal-footer">
        <button type="submit" class="btn btn-primary waves-effect">Save</button>
        <button type="button" class="btn btn-default waves-effect" data-dismiss="modal">Cancel</button>
    </div>
}
