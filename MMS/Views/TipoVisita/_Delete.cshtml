@model MMS.Models.TipoVisita
@{
    string controllerName = ViewContext.RouteData.Values["controller"].ToString();
}

<script src="~/Scripts/PerfectStore/TipoVisita.js"></script>

<script type="text/javascript">
    $.MMS.TipoVisita.init = function () {
        $.MMS.TipoVisita("delete");
    };
</script>

<div class="modal-header">
    <button type="button" class="close btn-link" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title">Delete Type of visit</h4>
</div>

<div class="modal-body" id="bodyForm">
    <div class="row clearfix">
        @Html.HiddenFor(model => model.Id)

        <div class="col-md-12">
            <div class="form-group form-float">
                <div class="form-line">
                    @Html.EditorFor(model => model.Nombre, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.LabelFor(model => model.Nombre, htmlAttributes: new { @class = "form-label" })
                </div>
            </div>
        </div>
    </div>
</div>

<hr class="m-b-0 m-t--10" />

<div class="modal-footer">
    @using (Html.BeginForm("Delete/" + Model.Id, "api/" + controllerName, FormMethod.Post, new { id = "frmTipoVisita" }))
    {
        <button type="submit" class="btn btn-danger waves-effect">Delete</button>
        <button type="button" class="btn btn-default waves-effect" data-dismiss="modal">Cancel</button>
    }
</div>